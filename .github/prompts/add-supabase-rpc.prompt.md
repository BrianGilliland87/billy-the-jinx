Create a safe Supabase RPC workflow for Billy the Jinx.

Requirements:
- prefer database-side integrity for balances and event transitions
- preserve auth checks
- preserve event lock/final rules
- include any needed policy/grant updates
- include matching client-side call examples